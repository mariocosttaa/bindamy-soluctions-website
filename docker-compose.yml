services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bindamy-website
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped

